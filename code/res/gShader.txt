#version 330
layout(triangles) in;
layout(triangle_strip, max_vertices = 6) out;
vec4 offset = vec4(5.5, 0, 0, 0);
void main()
{	
	for (int n = 0; n < 3; n++){
		gl_Position = gl_in[n].gl_Position; 
		EmitVertex();
	}
	EndPrimitive(); 
}